version: '3.8'
services:
  vector-database:
    image: pgvector/pgvector:pg16
    container_name: factory-vector-db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=garment_erp_db
    ports:
      - "5432:5432"
    restart: unless-stopped
